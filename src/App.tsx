import './App.css';

export function App() {
    return (
        <main className="app">
            <h1>Voter Sliders</h1>
            <p>
                Slide the demographics and watch the electoral map respond. This is the starting
                scaffold &mdash; the map and sliders are on the way.
            </p>
        </main>
    );
}
